Task: Redesign the MoodEntryItem composable in app/src/main/java/net/maiatoday/moodsnap/ui/history/MoodEntryItem.kt to match a more modern, data-dense Material 3 aesthetic.

Objective: Update the MoodEntryItem UI with the following layout and styling changes:

1. Header Layout:
       * Move the Date text to the top-right corner, aligned horizontally with the Description.
       * Use a smaller typography style for the Date.
2. Mood Description:
       * Remove the "Description:" label.
       * Increase the font size and use a Bold weight for the description text.
3. Mood Score & Emoji:
       * Center the Mood Score (number only) directly below the Emoji.
4. Tags:
       * Display tags in a single, horizontally scrollable row (e.g., LazyRow).
       * Add a leading tag/label icon to the left of the row.
       * Remove the "Tags:" prefix label.
5. Notes:
       * Restrict the Notes text to a maximum of two lines.
       * Use TextOverflow.Ellipsis for overflow.
       * Remove the "Notes:" prefix label.
6. Energy Progress:
       * Represent entryDomain.energy as a ratio of 15 (e.g., "7/15").
       * Add a LinearProgressIndicator below the text to visually represent this value.
7. Habit Indicators (Booleans):
       * For sleep, movement, and sunlight, use a FilterChip or a custom Pill Shape.
       * Filled state: If the boolean is true.
       * Outlined state: If the boolean is false.
8. Card Visuals:
       * Maintain the vertical colored bar on the left edge of the card (using mood.color).

Constraints:
   * Adhere strictly to Material 3 guidelines and existing project theme (Theme.kt).
   * Ensure the card remains clickable and responsive.
