Run the unit tests, and fix any errors. Add new unit tests for new classes. Prefer using fakes instead of mocks and continue to update the unit tests. Specifically expand the unit tests for SettingsScreenViewModel and add new unit tests for the changes added to the other layers e.g.  @ScheduleReminderUseCase.kt