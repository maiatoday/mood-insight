Here is a prompt I need to improve:
<prompt>
The MoodEntry in this app has tags but there is no way to show which tags are in the entry on the UI. There is also no way to add new tags. What do you suggest I do to improve this. I want to be able to see all the tags that are added to an entry. I want to be able to remove a tag. I want to be able to easily add tags that I have already created. I was thinking about toggleable pill buttons but not sure about the UX. Give me a few scenarios of how to fix this.
</prompt>
evaluate this prompt and rewrite it for clarity. Don't implement anything. Add reference to any files as needed. Also if you think it will help break the process into a series of that can be executed sequentiall so I can have save points inbetween. Include a first step to get the database into an appropriate state for this feature. Feel free to create any new tables or entities if necessary.