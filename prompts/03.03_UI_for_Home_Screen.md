Refactor the HomeScreen to match the design in @/.agent/resources/03-ui-scaffold/HomeScreen.jpg.

Replace the existing Placeholder content  with a UI that implements the following requirements. Ensure the implementation follows Clean Architecture (UI -> Domain -> Data).

# 1. UI Layer (Jetpack Compose)
- **Top Bar:** "Mood Snap" title with a Menu icon (left) and Settings icon (right).
- **Mood Graph:** A bar chart showing the daily mood score for the past 7 days.
- **Current Status (Right of/Next to graph):**
  - A large visual icon representing the current mood.
- **Averages (Below graph):**
  - Text: "Past week: [Average Mood Score]"
  - Text: "Energy: [Average Energy Score]" (displayed below the mood average).
- **Tags Section:**
  - Display tags used in the last 7 days (e.g., "Stress", "Junk Food").
  - **Layout:** Use a **horizontal scrolling row** (e.g., `LazyRow`) for the tags so they don't consume too much vertical space.
- **Habit Tracker:** Three rows (Movement, Sunlight, Sleep).
  - Each row displays 7 stars total.
  - The number of **filled** stars should equal the number of days in the past week where that habit was marked as true.
- **FAB:**
  - Keep the existing Floating Action Button in the bottom-right.
  - Retain its current icon (+) and `onClick` functionality (navigation).
- **Preview:** Create a Compose Preview populated with dummy data matching the sketch.

# 2. Domain Layer
- Create/Update a UseCase (e.g., `GetWeeklySummaryUseCase`).
- **Logic:**
  - **Mood Average:** Calculate average mood over the last 7 days (ignore days with no entry).
  - **Energy Average:** Calculate average energy over the last 7 days (ignore days with no entry).
  - **Habit Counts:** Count total days in the past 7 days where `movement`, `sunlight`, and `sleep` were true.
  - **Tags:** Aggregate unique tags used in the past 7 days.

# 3. Data Layer
- Ensure the Repository exposes a Flow returning entries for the last 7 days.